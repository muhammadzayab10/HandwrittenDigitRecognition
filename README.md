# HandwrittenDigitRecognition
Handwritten Digit Recognition is a deep learning project that identifies handwritten digits (0–9) from images using a convolutional neural network trained on the MNIST dataset. It performs preprocessing, training, and prediction to achieve high accuracy in digit classification.
